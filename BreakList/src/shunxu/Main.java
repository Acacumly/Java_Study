package shunxu;

import javafx.scene.Node;

public class Main implements List {
    @Override
    public boolean add(int element) {
        return false;
    }

    @Override
    public boolean add(int index, int element) {
        int size = 0;
        Node n = head;
        while( n; n !=  null; n = n.next ) {
            size++;
        }
        if(index > size - 1) {
            return;
        }
        if(z)

        return false;
    }

    @Override
    public int get(int index) {
        return 0;
    }

    @Override
    public int set(int index, int val) {
        return 0;
    }

    @Override
    public int remove(int index) {
        return 0;
    }

    @Override
    public int size() {
        return 0;
    }

    @Override
    public boolean isEmpty() {
        return false;
    }

    @Override
    public Iterator iterator() {
        return null;
    }
}

    /**
     * 找到index所在的结点
     * index一定是合法的,index一定不是()
     * 判断从前往后找,还是从后往前
     * @param index
     * @return
     * */
    private Node getNode(int index) {
        if(index < size / 2) {
            Node cur = head;

        }
    }